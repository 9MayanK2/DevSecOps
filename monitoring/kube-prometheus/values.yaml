grafana:
  enabled: true

  adminUser: admin
  adminPassword: admin123

  service:
    type: ClusterIP

  persistence:
    enabled: true
    type: sts
    storageClassName: gp2
    accessModes:
      - ReadWriteOnce
    size: 10Gi